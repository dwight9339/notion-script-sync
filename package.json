{
    "name": "sprongus",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "build:core": "pnpm --filter @sprongus/core run build",
        "build:cli": "pnpm --filter @sprongus/cli run build"
    },
    "devDependencies": {
        "eslint": "^9.30.0",
        "prettier": "^3.6.2",
        "typescript": "^5.8.3"
    }
}